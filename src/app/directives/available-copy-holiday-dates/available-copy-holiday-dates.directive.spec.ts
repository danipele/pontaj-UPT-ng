import { AvailableCopyHolidayDatesDirective } from './available-copy-holiday-dates.directive';

describe('AvailableCopyHolidayDatesDirective', () => {
  it('should create an instance', () => {
    const directive = new AvailableCopyHolidayDatesDirective();
    expect(directive).toBeTruthy();
  });
});
